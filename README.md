# custom-debian-live-creator
A bash script solution for preparing custom debian live media. It takes the official Debian standard ISO image and provides the user with a chroot environment to configure it before writing it to USB-media. The solution can handle different Live System variants for repeated use.
